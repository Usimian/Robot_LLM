# Unified Robot Controller Requirements
# Minimal dependencies for maximum efficiency

# Core web framework
Flask==2.3.3
Flask-SocketIO==5.3.6

# Async HTTP client (for robot communication)
aiohttp==3.9.1

# Image processing (same as before)
Pillow==10.1.0
opencv-python==4.8.1.78
numpy==1.24.3

# PyTorch (same as before, for VILA)
torch==2.1.1
torchvision==0.16.1

# Existing VILA dependencies
transformers==4.35.2
accelerate==0.24.1

# Standard library extensions
requests==2.31.0

# Optional: For better async performance
uvloop==0.19.0; sys_platform != "win32"